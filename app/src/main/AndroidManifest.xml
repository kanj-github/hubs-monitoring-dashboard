<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.altisource.hubzu.dashboard">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.IncidentProcessDetailActivity"
            android:launchMode="singleInstance"
            android:parentActivityName=".activity.NavigationDrawerActivity"/>

        <activity
            android:name=".ui.FailureInfoActivity"
            android:launchMode="singleInstance" />

        <activity
            android:name=".activity.NavigationDrawerActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".ui.PendingIncidentsActivity"
            android:launchMode="singleInstance"
            android:parentActivityName=".activity.NavigationDrawerActivity"/>

        <!-- Dummy activity for testing/demo -->
        <activity
            android:name=".dummy.MainActivity"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>